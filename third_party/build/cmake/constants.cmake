if (WIN32 AND MSVC)
	set(MAKE_TOOL "nmake")
	set(MAKE_CLEAN "nmake clean")
	if("${CMAKE_SIZEOF_VOID_P}" STREQUAL "8")
		set(OPENSSL_PLANTFORM "VC-WIN64A")
	else()
		set(OPENSSL_PLANTFORM "VC-WIN32")
	endif()
    set(OPENSSL_CONF_COMMAND "Configure")
elseif (UNIX)
	set(MAKE_TOOL "make")
	set(MAKE_CLEAN "make clean")
	set(MAKE_CONCURRENT_PARAM "-j")
else()
endif()

set(PERL "perl")
set(NO_ASM "no-asm")

